function convertToInteger(str) {

    return parseInt(str);
    // str değiskenini tam sayıya donusturduk.
  }
  
  // funct deger aldi ve console yazildi
  const result = convertToInteger("56");
  console.log(result); 